---
import Layout from '../../layouts/Layout.astro';

const token = Astro.url.searchParams.get('token') || '';
const email = encodeURIComponent(Astro.url.searchParams.get('email') || '');
---


<Layout>

</Layout>
